version: '3'
services:
  app:
    container_name: question-service-app
    restart: always
    build: .
    ports:
      - '5000:8002'
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017'